{% macro contentBlock(pageTitle, message) %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ pageTitle }}</h1>
      <p class="govuk-body">{{ message }}</p>
    </div>
  </div>
{% endmacro %}